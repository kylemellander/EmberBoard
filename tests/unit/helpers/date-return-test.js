import { dateReturn } from '../../../helpers/date-return';
import { module, test } from 'qunit';

module('Unit | Helper | date return');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateReturn(42);
  assert.ok(result);
});
